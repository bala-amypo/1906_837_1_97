spring.application.name=digital-certificate-generator

# Keep your required port
server.port = 9001
server.forward-headers-strategy=framework 

# Keep your environment database settings
spring.datasource.url=jdbc:mysql://localhost:3306/CertificateGenerator?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Note: In Spring Boot 3, it's safer to let Hibernate auto-detect the dialect
# but keeping your line is fine if the database is MySQL 8.

# ==========================================
# MANDATORY ADDITIONS FOR TEST CASES
# ==========================================

# JWT Properties (Required for JwtUtil constructor in Section 8.1)
jwt.secret=9a67471a2bc916670c538749a04473874052345d6255656241762354124a63
jwt.expiration=3600000

# Swagger / OpenAPI Paths (Required for Section 9.1)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html